import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#454e72",
    },
    neutral: {
      main: "#ffffff",
    },
    dark: {
      main: "#351b7e",
      contrastText: "#ffffff",
    },
  },
});
